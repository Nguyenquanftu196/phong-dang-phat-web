export const styles = (theme) => ({
  contentLoading: {
    position: 'fixed',
    background: 'rgba(0,0,0,0.6)',
    height: '100vh',
    width: '100%',
    top: 0,
    left: 0,
    zIndex: 1,
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center'
  }
});
